const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
const translateText=async(text,target)=>{const url="https://translate.googleapis.com/translate_a/single?client=gtx&sl=ru&tl="+encodeURIComponent(target)+"&dt=t&q="+encodeURIComponent(text);const response=await fetch(url);if(!response.ok)throw Error("Translation service error: "+response.status);const data=await response.json();return Array.isArray(data&&data[0])?data[0].map((part)=>Array.isArray(part)?String(part[0]||""):"").join(""):"";};
const translateValue=async(value,target)=>{if(Array.isArray(value)){const result=[];for(const item of value){const text=String(item||"").trim();result.push(text?await translateText(text,target):"");}return result;}const text=String(value||"").trim();return text?await translateText(text,target):"";};
export async function onRequestPost({request}){let payload;try{payload=await request.json();}catch{return json({error:"Invalid JSON"},400);}const target=String(payload&&payload.target||"");if(target!=="uz"&&target!=="en")return json({error:"Unsupported language"},400);const texts=payload&&payload.texts&&typeof payload.texts==="object"&&!Array.isArray(payload.texts)?payload.texts:{};const entries=Object.entries(texts).slice(0,40);const translations={};try{for(const entry of entries){const key=entry[0],value=entry[1];if(Array.isArray(value)){if(value.length>40)return json({error:"Too many list items"},413);for(const item of value)if(String(item||"").length>2000)return json({error:"Text is too long"},413);}else if(String(value||"").length>5000)return json({error:"Text is too long"},413);translations[key]=await translateValue(value,target);}}catch(error){return json({error:error instanceof Error?error.message:"Translation failed"},502);}return json({translations});}
